{
  "name": "blog",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && npx gh-pages -d dist -b gh-pages",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.7"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
